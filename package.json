{
  "name": "swg",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Salvatore <dested@gmail.com>",
  "license": "MIT",
  "scripts": {
    "setup": "cd common && npm install && cd ../server-common && npm install && cd ../api && npm install && cd ../web && npm install && cd ../worker && npm install",
    "tsc": "cd common && tsc && cd ../server-common && tsc && cd ../api && tsc && cd ../web && tsc && cd ../worker && tsc && cd ../lambda && tsc"
  },
  "dependencies": {
    "serverless": "^1.27.3"
  },
  "devDependencies": {
    "serverless-webpack": "^5.1.5",
    "typescript": "^2.9.2",
    "tslint": "^5.10.0",
    "tslint-config-prettier": "^1.13.0",
    "tslint-plugin-prettier": "^1.3.0",
    "prettier": "^1.13.5",
    "tslint-eslint-rules": "^5.3.1"
  }
}
